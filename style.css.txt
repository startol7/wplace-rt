:root{
  --bg:#0f1620; --panel:#121a26; --text:#e8eef7; --muted:#9fb0c5;
  --accent:#5aa0ff; --border:#243143; --ok:#2dd4bf;
}
*{box-sizing:border-box}
html,body{margin:0;height:100%;background:var(--bg);color:var(--text);font-family:system-ui,Segoe UI,Helvetica,Arial,sans-serif}
.hidden{display:none!important}

.appbar{display:flex;justify-content:space-between;align-items:center;padding:.6rem 1rem;border-bottom:1px solid var(--border);background:#0c121a;gap:.8rem;flex-wrap:wrap}
.brand{font-weight:800;letter-spacing:.3px}
.controls{display:flex;gap:.5rem;align-items:center}
.country-label{font-size:.9rem;color:var(--muted)}
select{background:#0d1520;color:var(--text);border:1px solid var(--border);border-radius:8px;padding:.35rem .5rem}
button{border:0;border-radius:10px;background:var(--accent);color:white;padding:.45rem .7rem;cursor:pointer;font-weight:600}
button.ghost{background:transparent;border:1px solid var(--border);color:var(--text)}
button:disabled{opacity:.5;cursor:not-allowed}

.main{display:grid;grid-template-columns:1fr 350px;gap:12px;padding:12px}
.map-wrap{position:relative}
#map{width:100%;height:72vh;border:1px solid var(--border);border-radius:12px;overflow:hidden}
.cooldown{position:absolute;left:12px;bottom:12px;background:#0b1220cc;border:1px solid var(--border);border-radius:10px;padding:.5rem .7rem;display:flex;gap:.6rem;align-items:center}
.cooldown .bar{width:220px;height:8px;background:#0d1a2a;border-radius:999px;overflow:hidden}
.cooldown .fill{height:100%;background:var(--ok);width:0%}

.side{display:flex;flex-direction:column;gap:12px}
.panel{background:var(--panel);border:1px solid var(--border);border-radius:12px;padding:12px}
.panel-title{font-weight:800;margin-bottom:.4rem}
.seg{display:flex;gap:6px;margin:.4rem 0}
.seg-btn{background:#0d1520;border:1px solid var(--border);color:var(--text)}
.seg-btn.active{background:var(--accent)}
.row{display:flex;gap:8px;align-items:center;margin:.4rem 0}
.row input[type="text"]{background:#0d1520;border:1px solid var(--border);color:var(--text);padding:.35rem .5rem;border-radius:8px;width:120px}
.list{list-style:none;margin:0;padding:0}
.list li{display:flex;justify-content:space-between;padding:.35rem 0;border-bottom:1px dashed #1e2a3b}
.kv{display:flex;justify-content:space-between;border-bottom:1px dashed #1e2a3b;padding:.35rem 0}
.footer{padding:.8rem 1rem;border-top:1px solid var(--border);color:var(--muted)}

@media (max-width: 880px){
  .main{grid-template-columns:1fr;gap:10px}
  #map{height:60vh}
}
