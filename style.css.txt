body { margin:0; font-family: system-ui, sans-serif; background:#0d1117; color:#eee; }
.header { display:flex; justify-content:space-between; align-items:center; padding:8px 12px; background:#161b22; }
.logo { font-size:18px; font-weight:700; display:flex; align-items:center; gap:6px; }
.stats { display:flex; gap:12px; }
.stat-item { text-align:center; }
.stat-value { font-size:16px; font-weight:700; }
.main-container { display:flex; height:calc(100vh - 50px); }
#map { flex:1; }
.sidebar { width:320px; background:#161b22; overflow-y:auto; padding:8px; }
.panel { background:#1f242d; border-radius:10px; padding:8px 12px; margin-bottom:10px; }
.control-buttons { position:absolute; right:16px; bottom:16px; display:flex; flex-direction:column; gap:8px; }
.control-btn { width:52px; height:52px; border-radius:50%; border:none; font-size:20px; cursor:pointer; }
.cooldown-overlay { position:absolute; bottom:0; left:0; right:0; padding:4px; background:#222; display:flex; align-items:center; gap:8px; }
.cooldown-progress { flex:1; background:#444; border-radius:6px; overflow:hidden; height:12px; }
.cooldown-fill { background:#4ade80; width:0; height:100%; transition:width 0.1s; }
.notification { position:fixed; top:10px; right:10px; padding:8px 12px; border-radius:8px; display:none; }
.notification.show { display:block; }

/* Buttons */
.btn {
  height: 32px; padding: 0 10px; border-radius: 8px; border: none; cursor: pointer;
  background: rgba(255,255,255,.9); font-weight: 700; box-shadow: 0 2px 6px rgba(0,0,0,.2);
}
.btn:hover { transform: translateY(-1px); }
.btn.ghost { background: rgba(255,255,255,.2); color: #fff; border: 1px solid rgba(255,255,255,.25); }
.btn.block { width: 100%; }
.color-row { display: flex; gap: 6px; align-items: center; margin:6px 0; }
.color-row input[type="color"] { width: 42px; height: 32px; padding: 0; border: none; }
.grid-2 { display: grid; grid-template-columns: 1fr 1fr; gap: 6px; }
.hint { font-size: 12px; opacity: .7; margin:4px 0; }

/* Mobile */
@media (max-width: 980px) {
  .main-container { flex-direction: column; height:auto; min-height:calc(100vh - 50px); }
  #map { height:60vh; min-height:300px; }
  .sidebar { width:100%; height:auto; }
  .control-buttons { right:12px; bottom:12px; }
  .control-btn { width:48px; height:48px; font-size:22px; }
}
.sidebar.collapsed { display:none; }
